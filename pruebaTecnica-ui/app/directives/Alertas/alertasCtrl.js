'use strict';
angular.module('pruebaTecnicaApp')

.controller('AlertasController',
		[ '$scope', '$filter', function($scope, $filter) {
			$scope.init = function() {
			};

			$scope.init();

		} ]);
